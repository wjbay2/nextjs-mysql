import { Layout, AddEditForm } from 'components/products';

export default function Add() {
    return (
        <Layout>
            <h1>Add Product</h1>
            <AddEditForm />
        </Layout>
    );
}